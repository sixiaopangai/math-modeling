# 项目背景与硬性约束

## 一、项目是什么

2026 年校级数学建模竞赛，赛题引自 APMCM 亚太赛 A 题《自来水厂水质预测与评估》。数据是某水厂 2025-01 至 2026-03 的运行监测记录，每 2 小时一条，重建后 5460 行、77 个字段。四问：

| 问题 | 任务 | 交付 |
|---|---|---|
| Q1 | 筛选影响出厂水浊度的主要因素，建立函数关系，预测 2026-02-01/10/20 三日浊度 | Excel 表 + 多模型对比 |
| Q2 | 建立原水指标与操作变量到滤后水浊度的动态时滞模型 | 各输入时滞 + RMSE/R² |
| Q3 | 质量守恒（清水池 RTD）+ 数据驱动的混合模型，预测未来 1–12 h 出厂浊度 | 三日 07:00–19:00 预测 + 敏感性 |
| Q4 | 以 1 NTU 为硬约束，按超标幅度与持续时长建立四级风险评价 | 等级占比 + 3 月逐日分类 |

校赛明确「参赛论文的结果不作为评判的核心标准」，重点是建模流程、验证严谨性和论文写作。因此**不要为了让指标好看而调整模型或口径**；负面证据（负 R²、基线优胜、残差相关）是刻意保留的交付内容。

## 二、仓库现状

主体已完成，处于收尾阶段。详细的复现步骤、环境版本与产物对应关系见 `README.md`。

| 路径 | 内容 |
|---|---|
| `water_quality_model.py` / `water_quality_full.py` | 核心计算：数据加载、四问建模 |
| `water_quality_figures.py` | 绘图 |
| `reproduce.py` | 全量复现入口 |
| `diagnose_shift.py` / `diagnose_figure.py` | 事后诊断，只读取冻结结果，不参与模型选择 |
| `paper/完整论文-LaTeX/main.tex` | 论文源码（编译出 32 页 PDF） |
| `results/建模结果.xlsx` | 题目要求的 Excel 答案表，17 个 sheet |
| `results/*.csv` `*.json` | 中间结果与门禁记录 |
| `figures/` | 论文插图（12 幅正式图 + 1 幅诊断图） |
| `data/附件/` | **唯一权威数据源**（代码读取路径，只读） |
| `docs/题目分析报告.md` | 建模前的方案设计文档，含预先登记的判定规则 |
| `docs/T1诊断分析报告.md` | 外部验证失败的事后诊断 |

复现命令：`PYTHONPATH=.vendor python reproduce.py`。运行前 `data/` 下所有文件必须为只读，否则输入保护门禁失败。

## 三、红线（任何情况下不得突破）

1. **2026 年 3 月数据不得参与任何超参数选择或模型筛选**，仅用于最终外部报告。
2. **问题一诊断出的 −0.3433 NTU 系统偏移是诊断结论，不是可回填进模型的参数。** 它由三月实测算得，用它校正三月预测即自欺；且二月目标整段缺失，该校正在二月本就无法实施。
3. 时间划分固定：训练/开发 = 2025-01 至 2026-01；最终预测 = 2026-02（目标全缺失）；外部验证 = 2026-03。
4. 每问模型体系上限 2 套，不得为凑指标新增第三套。
5. 随机种子固定 `20260727`。
6. 原始附件只读，任何输出不得写入 `data/`。
7. 阈值表述必须区分「国标硬约束 ≤1 NTU」与「本题设计的四级运营阈值」，后者不得冒称国家标准。
8. 相关性表述不得写成因果性。
9. 修改论文前备份 `paper/完整论文-LaTeX/main.tex`（备份放 `backups/`）；改后必须重新编译验证（0 error、0 undefined、0 overfull/underfull）。

## 四、易踩的坑

- **论文与 `results/` 必须逐一对应。** 改论文数值前先确认对应的 CSV/JSON；改代码后重跑 `reproduce.py` 并同步论文里引用的计数（如工作簿公式数）。
- **可辨识性判据分两层**：`docs/题目分析报告.md` §5.2 的退化判据（众数占比 ≥90% 或取值恒定）与 §5.3 的 ±4 h 互相关容差，两者都通过才算可辨识。原水 pH 已按此判为不可辨识。
- **图件门禁**：`figures/` 下每个 stem 必须 PNG + SVG 配对，PNG ≥300 dpi，SVG 必须保留可编辑文本节点（`svg.fonttype = "none"`）。
- **BibTeX 与 `-outdir`**：部分发行版会在输出目录里找不到 `references.bib`，参考文献全空但编译仍返回 0。设 `BIBINPUTS` 指向项目目录，并以「日志 undefined 计数为 0、PDF 32 页」验收。
- **跨平台浮点**：Linux 与 Windows 之间有 1e-13 量级差异，`plot_*` 与 npz 的字节哈希会变，但论文引用的 CSV 逐字节一致。
